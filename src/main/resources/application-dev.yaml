spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ncdc-ica-heritage
    username: postgres
    password: ${DB_PASSWORD:Aditya623880}
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect

    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        default_schema: public
